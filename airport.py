
class Airport:
    def __init__(self, name: str, code: str, lat: float, long: float):
        self.name = name
        self.code = code
        self.lat = lat
        self.long = long

     #  def info(self):
      #  print(
           # f"Bonjour, c'est la l'éroport {self.name} code {self.code} sur l'altitude {self.lat} avec une longitude {self.long}.")
